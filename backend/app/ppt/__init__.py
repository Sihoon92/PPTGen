"""PPT generation domain logic.

Pure, framework-free modules: Slide DSL / Layout IR models, deterministic
validator and compiler, theme resolution, and the Node-sidecar renderer bridge.
Importable without LangGraph so the pipeline pieces can be unit-tested in isolation.
"""
